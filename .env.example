# NEXI Assistant - Environment Configuration
# Copia este archivo a .env y completa con tus valores reales.
# El archivo .env NUNCA debe subirse a GitHub (está en .gitignore).

# Supabase (obligatorio)
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=tu_anon_key_aqui

# Organización y sesión (opcional, valores por defecto: MILTON_ORG_ID, nexi_primary)
ORGANIZATION_ID=MILTON_ORG_ID
SESSION_ID=nexi_primary
